# Core
Flask>=3.0,<4.0
bcrypt>=4.1
cryptography>=42.0        # used only by generate_certificates.py (TLS cert)

# Optional real-time delivery. Without it the app falls back to HTTP polling.
flask-socketio>=5.3

# Tests
pytest>=8.0
