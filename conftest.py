# Presence of this file at the repo root puts the root on sys.path during test
# collection, so `tests/test_server.py` can `import simple_secure_server`
# regardless of how pytest is invoked (`pytest` vs `python -m pytest`).
